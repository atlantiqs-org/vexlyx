/**
 * @vexlyx/shared — Shared types, schemas, and constants for the Vexlyx monorepo.
 *
 * This package is imported by both `apps/dashboard` and `apps/api` to ensure
 * type safety across the frontend and backend.
 *
 * Zod schemas and inferred types will be added here in F0.7.
 */

/** Current version of the Vexlyx platform */
export const VEXLYX_VERSION = "0.0.1";

/** Application name constant used across both frontend and backend */
export const APP_NAME = "Vexlyx";
