{
  "name": "@vexlyx/api",
  "version": "0.0.1",
  "private": true,
  "description": "Vexlyx API server (Fastify)",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --build",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@vexlyx/shared": "workspace:*",
    "fastify": "^5.12.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
