/**
 * Vexlyx API — Entry point placeholder.
 *
 * This file will be replaced in F0.3 with the Fastify server setup.
 * For now, it validates that workspace imports from @vexlyx/shared work correctly.
 */

import { APP_NAME, VEXLYX_VERSION } from "@vexlyx/shared";

console.warn(
  `${APP_NAME} API v${VEXLYX_VERSION} — placeholder (replace in F0.3)`,
);
