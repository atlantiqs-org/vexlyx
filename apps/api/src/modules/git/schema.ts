import { ConnectRepoSchema } from "@vexlyx/shared";

// Re-export the shared schema so routes can import from a single local path.
export { ConnectRepoSchema };
export type { ConnectRepoInput } from "@vexlyx/shared";
