/**
 * Vexlyx Dashboard — Entry point placeholder.
 *
 * This file will be replaced entirely in F0.2 when Next.js is scaffolded.
 * For now, it validates that workspace imports from @vexlyx/shared work correctly.
 */

import { APP_NAME, VEXLYX_VERSION } from "@vexlyx/shared";

console.warn(
  `${APP_NAME} Dashboard v${VEXLYX_VERSION} — placeholder (replace in F0.2)`,
);
