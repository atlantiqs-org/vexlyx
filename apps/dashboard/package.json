{
  "name": "@vexlyx/dashboard",
  "version": "0.0.1",
  "private": true,
  "description": "Vexlyx Dashboard (Next.js)",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --build",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@vexlyx/shared": "workspace:*"
  }
}
