# Nginx site config for static/SPA projects (F5.7 — no-build deploy path).
# Generated by docker_manager.py -- do NOT edit manually.

server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
